// config.ts
const config = {
    apiBaseUrl: 'http://localhost:8080/api/v1',  // URL base de tu API en Django
    endpoints: {
      obtenerDatos: '/data',
      // Agrega más endpoints según sea necesario
    }
  };
  
  export default config;